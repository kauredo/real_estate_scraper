<div class="container mx-auto flex flex-col sm:flex-row px-4 flex-wrap mb-6">
  <div class="w-full shadow-md rounded px-2 sm:px-8 py-4 mt-4 dark:border-beige-medium dark:border-2">
    <h2 class="text-2xl font-bold leading-7 text-dark dark:text-light  text-center sm:text-3xl">Editar Empreendimento: <%= @listing_complex.name %></h2>
    <%= form_for([:backoffice, @listing_complex], html: { method: :put, class: 'my-6' }) do |f| %>
      <%= render 'form', f: %>
    <% end %>
  </div>
</div>
