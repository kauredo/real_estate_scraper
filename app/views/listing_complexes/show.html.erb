<% content_for(:meta_title) { @listing_complex.name } %>
<% content_for(:meta_image) { @listings.first.photos.first } %>
<%= react_component 'listingComplex--Show', { complex: @listing_complex, listings: @listings }  %>
