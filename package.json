{
  "private": true,
  "scripts": {
    "api": "cd api && bundle exec rails s -b 0.0.0.0",
    "frontend": "cd frontend && npm run dev",
    "dev": "npm-run-all --parallel api frontend"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "npm-run-all": "^4.1.5"
  }
}
