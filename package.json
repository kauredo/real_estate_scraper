{
  "private": true,
  "scripts": {
    "api": "cd api && bundle exec rails s -b 0.0.0.0",
    "frontend": "cd frontend && npm run dev",
    "jobs": "cd api && bundle exec good_job start",
    "dev": "npm-run-all --parallel api frontend jobs",
    "start": "cd api && bundle exec rails s -b 0.0.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "npm-run-all": "^4.1.5"
  }
}
