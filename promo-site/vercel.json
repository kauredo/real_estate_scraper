{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "bash -c 'if git diff HEAD^ HEAD --quiet -- promo-site/; then exit 0; else exit 1; fi'"
}
